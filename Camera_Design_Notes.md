# 镜头设计笔记

## 相机臂（Camera Arm / Spring Arm）设计

> 参考 UE Spring Arm + 参考项目 UCameraController.cs

相机臂 = 相机和角色之间一段虚拟的"杆"，绕着角色转，保持固定距离。

### 核心公式

```csharp
// 鼠标累计角度 → 绕角色旋转 → 相机永远在角色为中心的球面上
yMouse += Input.GetAxis("Mouse Y");      // 俯仰角（上下）
xMouse += Input.GetAxis("Mouse X");      // 偏航角（左右）
Quaternion rot = Quaternion.Euler(yMouse, xMouse, 0);
camera.position = target.position + rot * new Vector3(0, offsetY, -distance);
camera.rotation = rot;
```

这个公式保证：
- 相机**永远以角色为中心**旋转
- 相机**保持固定距离**（distance 不变，只变角度）
- 鼠标左右转 → 相机绕角色水平转
- 鼠标上下转 → 相机上下俯仰

### 相机臂的四个组成

| 组件 | 作用 | 对应代码 |
|------|------|---------|
| **枢轴点（Pivot）** | 相机绕谁转 | `target.position + offsetY`（角色中心上方） |
| **臂长（Arm Length）** | 相机离角色多远 | `-distance`（Z 方向负值 = 在角色身后） |
| **臂朝向（Arm Rotation）** | 相机当前角度 | `Quaternion.Euler(yMouse, xMouse, 0)` |
| **臂碰撞（Collision）** | 碰墙缩短臂 | 后文四大难点·碰撞 |

### 相机臂 vs 直接移动相机

| | 相机臂 | 直接移动相机 |
|------|--------|------------|
| 角色中心保持 | ✅ 永远中心 | 容易漂移 |
| 旋转自然 | ✅ 绕半径转 | 直线移动生硬 |
| 碰墙处理 | ✅ 缩短臂长 | 容易穿墙 |
| 加 Spring | ✅ 好加 | 难加 |



## 球体投射（Sphere Cast）—— 相机碰撞处理

> `Physics.SphereCast` — Unity 内置方法，从一个点往一个方向扫一个球，返回第一个碰到的碰撞体。

### 为什么用球体而不是射线

```
射线 (Raycast)：                   球体 (SphereCast)：
──────→                             ══◉══→
一根无限细的线                       一个实心球扫过去

射线穿过墙角 → 相机边缘还是穿模      球有半径 → 整颗相机都不会穿
```

相机本身有体积（Near Clip Plane 到 Far Clip Plane），射线检测只保护了相机中心点，边缘照样穿墙。球体投射用相机的 Near Clip 半径做球半径，保证整颗相机都不会穿进去。

### 相机碰撞流程

```
1. 算出理想相机位置：smoothPivot + rot * (0, 0, -desiredArmLength)
2. 从 pivot 往理想位置方向做 SphereCast（半径 = 0.2~0.3m）
3. 没碰到 → 用 desiredArmLength
4. 碰到了 → armLength = hit.distance（缩到碰撞点前）
   （还要预留一点空隙，不然镜头贴墙上很丑）
```


### 和现有 SmoothDamp 配合

碰撞算出来的 `armLength` 会覆盖 `desiredArmLength`，SmoothDamp 自然产生弹性过渡——碰墙时迅速缩短、离开后平滑恢复。不需要额外写缓动。

| 参数 | 建议值 | 说明 |
|------|--------|------|
| `sphereRadius` | 0.2~0.3 | 相机 Near Clip 半径，别太大否则离墙太远 |
| `collisionMask` | ~Player | 排掉角色自己的碰撞层，否则相机永远被角色挡住 |
| `预留空隙` | sphereRadius | 贴太近镜头会进墙，留一个球半径的距离 |


---

## 镜头抖动（Impulse / Camera Shake）

### 抖动分量

| 分量 | 效果 | 例子 |
|------|------|------|
| **仅位置** (Position) | 画面上下左右晃动 | 受击、脚步落地 |
| **仅旋转** (Rotation) | 画面倾斜抖动 | 爆炸冲击波 |
| **位置+旋转** | 最强烈 | 大招落地、地震 |

### 抖动参数

| 参数 | 小幅度 | 中幅度 | 大幅度 |
|------|--------|--------|--------|
| 振幅 (Amplitude) | 0.1-0.3 | 0.3-0.7 | 0.7-1.5 |
| 频率 (Frequency) | 0.5-1s | 0.2-0.5s | <0.2s |
| 衰减 (Dissipation) | 快 (<0.3s) | 中 (0.3-0.8s) | 慢 (0.8-2s) |

### 原神/鸣潮参考

| 效果 | 振幅 | 频率 | 衰减 |
|------|------|------|------|
| 普攻命中 | ~0.1 | 0.5s | 0.1s |
| 重击命中 | ~0.3 | 0.3s | 0.2s |
| 元素爆发 | ~0.8 | 0.2s | 0.5s |
| 角色被击飞 | ~1.0 | 0.15s | 0.8s |
| 走路（Noise） | ~0.05 | 1s | 持续 |

---

## 动态镜头与静态镜头

### 定义

| 类型 | 特征 | 例子 |
|------|------|------|
| **静态镜头** | 相机位置和朝向固定，角色在固定框内表演 | 剧情对话、大招特写、BOSS 出场 |
| **动态镜头** | 相机持续跟随角色位置/朝向变化 | 平常跑图、普攻连段、普通移动 |

### 动态 ↔ 静态 的转换时机

```
静态 ← 技能开始（拉近 + 固定机位）
  │
  ├─ 技能播完 → 转回动态（Ease Out，先快后慢，自然滑回）
  │
动态 ← 平常控制
  │
  ├─ 触发剧情/BOSS → 强制切静态（0s 硬切 or Ease In 0.3s）
  │
  ├─ 开启菜单/对话 → 切静态（软过渡 0.3-0.5s）
```

### 静态镜头的子类型

| 类型 | 相机行为 | 用在哪 |
|------|---------|--------|
| **完全锁死** | 相机完全不动 | BOSS 出场特写、剧情关键帧 |
| **LookAt 锁死** | 位置不动但追踪角色旋转 | 角色做大动作（大招）但机位固定 |
| **半锁** | 位置有弹性区域，出框才跟 | 狭窄场景、走廊 |

### 动态镜头的子类型

| 类型 | 相机行为 | 用在哪 |
|------|---------|--------|
| **紧跟随** | 零延迟、零阻尼 | 冲刺、快速移动 |
| **弹性跟** | 有阻尼、"肉"一点 | 日常移动、走路 |
| **预测跟** | 相机往角色移动方向提前偏一点 | 跑图、开放世界 |

### 两者关系

- **"谁主导"决定感受**：动态为主 → 自由感（原神跑图）；静态为主 → 电影感（《战神》一镜到底）
- **切换成本**：动态 → 静态时观众注意力在"框里发生了什么"；静态 → 动态时注意力在"我要去哪"
- **过度设计陷阱**：不要每个技能都切静态——频繁切换会晕。只在大招、处决、入场/离场用静态

### 原神/鸣潮参考

| 场景 | 原神 | 鸣潮 |
|------|------|------|
| 普通移动 | 动态紧跟随，LookAt 前方 3m | 动态弹性跟，有缓冲区 |
| 元素爆发 | 静态近景，LookAt 锁角色 | 静态 + 拉远再拉近 |
| 剧情对话 | 静态对切（正反打），0s 硬切 | 静态 + 慢 Blend |
| 锁敌战斗 | LookAt 切到敌人 | 类似，但鸣潮的弹性区还在 |
| BOSS 入场 | 完全锁死 2-3s | 拉远+慢转+临近停 |

---

## TP 相机开发四大核心难点（进阶）

> 来源：《Real-Time Cameras》第 7 章
> 前提：先掌握上面的"相机臂设计"，这些是相机臂做好之后的进阶问题

### 1. 碰撞与场景漫游

相机不能穿墙、穿模型。角色加速/跳跃时极易相机穿插。

- **动态插值**：检测到障碍物时，相机沿圆弧缩到离角色更近的位置（而不是穿过去）
- **预定义机位**：狭窄场景预埋固定相机位置，碰撞时切过去

### 2. 遮挡问题

| 等级 | 表现 | 处理 |
|------|------|------|
| **局部遮挡** | 小物体遮住角色头部 | 相机微调偏移 |
| **临时遮挡** | 队友/NPC 穿过画面 | 短暂透明化遮挡物 |
| **完全遮挡** | 墙体/地形遮死角色 | 致命——必须做相机规避逻辑（圆弧内缩或切预埋机位） |

### 3. 相机限位约束

- **通用限制**：障碍物碰撞 + BOSS 战强制固定镜头（禁用玩家自由旋转）
- **锁敌限制**：锁定观察目标时，直接禁用玩家自由旋转相机，强制跟敌

### 4. 相机朝向 / 前视设计

主流 ARPG 方案：相机固定斜上方跟随角色，角色移动时画面下方是角色、上方是前方空间。

- 高速移动时前视空间充足，方便预判敌人进攻
- 参考：原神默认相机——角色在画面下方约 1/3 处，上方 2/3 留给前方视野
- 详见《Real-Time Cameras》第 7 章

---

## 待补充

- FOV 缩放时机
- Dutch Tilt 倾斜
- Depth of Field 景深
- Noise 手持晃动
- 锁敌镜头切换

---
