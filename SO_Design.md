# ScriptableObject 设计思路

## 核心原则：**改什么东西就动什么文件**

硬编码地狱是"改一个数值要翻三个文件"，SO 分家的目标是"改一个东西只动一个地方"。

## 什么该进 SO、什么不该进

| 数据类型 | 存哪里 | 原因 |
|---------|--------|------|
| 策划改的数值（攻击力、血量） | Excel → SO | 策划不碰 Unity |
| 美术调的曲线（位移速度、晃屏强度） | Inspector SO | Excel 存不了 AnimationCurve |
| 只有运行时才确定的值（当前血量、CD 剩余） | 代码字段 | SO 是模板不是实例 |
| 多个角色共用的东西 | 一个 SO，各自拖引用 | 改一次全部生效 |
| 只有一个角色用的东西 | 角色专属 SO | 不污染公共池 |

## 你的项目 SO 分家汇总

| SO | 管什么 | 为什么独立 |
|----|--------|-----------|
| **StateSO** (Excel→生成) | 状态切换逻辑：窗口、目标状态 | 策划配表，跟表现无关 |
| **StateMotionSO** | 攻击位移：曲线、力度、时间段 | AnimationCurve 存不进 Excel |
| **StateEffectSO** | 特效：prefab、挂点、触发秒 | 美术拖资源，跟策划无关 |
| **StateWeaponSO** | 武器显隐：时间段 | 改武器不碰特效 |
| **MoveConfigSO** | 移动速度：按状态区分走/跑 | 数值调优频率高 |
| **CharacterFormSO** (后续) | 形态数据：双形态碰撞体 | 只有双形态角色用 |

## 怎么决定拆不拆

问自己三个问题：

1. **改这个的人跟改旁边那些的是同一个人吗？** 策划改攻击窗口 vs 你调位移曲线 → 分家
2. **改这个东西的频率跟旁边一样吗？** 移动速度每周调 vs 特效配完基本不动 → 分家
3. **这个东西有类型在 Excel 里存不了吗？** AnimationCurve、GameObject 引用 → 必须进 SO

三个问题只要有一个答"不同"，就该拆。

## 面试时怎么讲这套东西

不用背设计模式名字，就说一句话：

> "我每个 SO 只负责一件事——策划、美术、程序各改各的文件，互不打断。Excel 管'什么时候发生'，SO 管'发生时长什么样'，代码管'怎么发生'。"

面试官听到这句，已经知道你比大部分写硬编码的应届生多了一层架构意识。
